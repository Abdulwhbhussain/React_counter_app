# React_counter_app
1. Create a simple React counter app:     - Display counter value     - Increment/decrement counter using buttons 2. Use props to pass data from parent to child component 3. Use state to manage counter value
